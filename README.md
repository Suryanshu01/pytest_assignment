# API Test Automation with Pytest

## Description
This project contains automated tests for the `/posts` endpoint of the JSONPlaceholder public REST API.
The tests are written in Python using pytest and validate basic CRUD operations.

## Tech Stack
- Python
- pytest
- requests

## Installation

1. Create a virtual environment (optional but recommended)
2. Install dependencies:

```bash
pip install -r requirements.txt
